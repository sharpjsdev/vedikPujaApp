import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.vedikpuja.app',
  appName: 'VedikPuja',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  },
};

export default config;
